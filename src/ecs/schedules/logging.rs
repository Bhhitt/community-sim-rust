use legion::systems::Builder;
use log;

pub fn add_agent_logging_systems(builder: &mut Builder) {
    // log::debug!("[ECS_SCHEDULE] About to add agent_arrival_logging_system");
    // builder.add_system(crate::ecs::systems::agent_logging::agent_arrival_logging_system());
    // builder.flush();
    // log::debug!("[ECS_SCHEDULE] Finished agent_arrival_logging_system");

    // log::debug!("[ECS_SCHEDULE] About to add agent_move_logging_system");
    // builder.add_system(crate::ecs::systems::agent_logging::agent_move_logging_system());
    // builder.flush();
    // log::debug!("[ECS_SCHEDULE] Finished agent_move_logging_system");

    // log::debug!("[ECS_SCHEDULE] About to add agent_spawn_logging_system");
    // builder.add_system(crate::ecs::systems::agent_logging::agent_spawn_logging_system());
    // builder.flush();
    // log::debug!("[ECS_SCHEDULE] Finished agent_spawn_logging_system");

    // log::debug!("[ECS_SCHEDULE] About to add interaction_event_logging_system");
    // builder.add_system(crate::ecs::systems::interaction_event_logging::interaction_event_logging_system());
    // builder.flush();
    // log::debug!("[ECS_SCHEDULE] Finished interaction_event_logging_system");
}
