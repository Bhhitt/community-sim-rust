use legion::systems::Builder;
use log;

pub fn add_agent_event_log_bridge_system(builder: &mut Builder) {
    // log::debug!("[ECS_SCHEDULE] About to add agent_event_log_to_gui_system");
    // builder.add_system(crate::agent::event_log_bridge::agent_event_log_to_gui_system());
    // builder.flush();
    // log::debug!("[ECS_SCHEDULE] Finished agent_event_log_to_gui_system");
}
